
name: Pytest omc (1.13.2)

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Docker
        uses: ./.github/actions/pytest-omc1.13.2
